{
  "name": "@scripts/monkey-copymanga-enhance",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "license": "MIT",
  "scripts": {
    "build": "monkey-build",
    "clean": "rimraf dist",
    "test": "vitest run",
    "lint": "concurrently -m 1 --timings npm:lint:*",
    "lint:type-check": "tsc -p tsconfig.json --noEmit",
    "lint:formatter": "oxfmt --check",
    "lint:oxlint": "oxlint",
    "fix": "concurrently -m 1 --timings npm:fix:*",
    "fix:formatter": "oxfmt"
  },
  "dependencies": {
    "classcat": "^5.0.5"
  },
  "devDependencies": {
    "@scripts/flow": "1.0.0",
    "@scripts/gen-scroll-to": "1.0.0",
    "@scripts/gen-storage": "1.0.0",
    "@scripts/gm-polyfill": "1.0.0",
    "@scripts/gm-vue": "1.0.0",
    "@scripts/monkey-build": "1.0.0",
    "@scripts/throttle": "1.0.0",
    "concurrently": "^8.2.2",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.58.0",
    "rimraf": "^6.1.3",
    "vitest": "^4.1.0"
  }
}
