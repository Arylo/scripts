# CLAUDE.md

## 子包说明

- 子包路径：`apps/caddyfile-sdk`
- 包名：`@arylo/caddyfile-sdk`
- 目标：解析、操作和生成Caddyfile

## 文档

- 数据结构 ./docs/data_structure.md

## 提交流程

1. 在子包目录执行 `npm run lint`。
2. 在子包目录执行 `npm run test`。
3. 在子包目录执行 `npm run build`。
4. 以上步骤全部通过后再提交。
