# 场景二：为分享盘新建提取码

**前置条件**：已存在至少一个分享盘（`pan_id`）。

**步骤**：
1. 向 `POST /api/admin/pans/:pan_id/codes` 发送请求（无需请求体）。
2. 断言响应状态码为 `200`。
3. 断言响应体中包含新建的提取码记录，`code.value` 为 8 位字母数字字符串。
4. 向 `GET /api/admin/codes` 确认新提取码出现在列表中。
