# 场景三：获取分享盘详情

**前置条件**：已存在至少一个分享盘（`pan_id`）。

**步骤**：
1. 向 `GET /api/admin/pans/:pan_id` 发送请求。
2. 断言响应状态码为 `200`。
3. 断言响应体结构：
   ```json
   {
     "code": 200,
     "data": {
       "id": "...", "active": 1,
       "codes": [...],
       "perms": [...],
       "docs": [...]
     }
   }
   ```
