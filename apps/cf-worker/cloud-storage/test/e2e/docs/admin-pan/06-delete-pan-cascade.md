# 场景六：删除分享盘（级联清理）

**前置条件**：已存在分享盘，且该盘下关联了提取码、权限、文件。

**步骤**：
1. 向 `DELETE /api/admin/pans/:pan_id` 发送请求。
2. 断言响应状态码为 `200`。
3. 向 `GET /api/admin/pans/:pan_id` 确认返回 `404`（盘已不存在）。
4. 验证关联的提取码、权限记录也已被清理（通过 `GET /api/admin/codes` 等接口确认）。
