# 场景二十二：选择图片文件后可正常预览

**前置条件**：取件成功，分享盘中存在一个图片文件（mimetype 为 `image/*`），文件名已知。

**步骤**：
1. 在文件列表页（`/pan/:code`）点击该图片文件名，跳转至预览页（`/pan/:code/:hash`）。
2. 断言页面主区域渲染出 `<img>` 元素，`src` 指向 `/api/raw/:file_name`。
3. 断言图片加载完成（`naturalWidth > 0`）。
4. 断言页面不显示「无法预览」之类的降级提示。
