# 场景十八：文件名含特殊字符时下载正常

**前置条件**：分享盘中存在文件名含中文或特殊字符的文件（如 `测试 文件 (1).zip`）。

**步骤**：
1. 携带 Cookie，向 `GET /api/raw/测试 文件 (1).zip`（URL 编码后）发送请求。
2. 断言响应状态码为 `200`。
3. 断言响应头 `Content-Disposition` 中文件名经正确 URL 编码，可被浏览器解析为原始文件名。
