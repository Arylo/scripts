declare const Vue: typeof import('vue')

export const {
  createApp,
  defineComponent,
  readonly,
  ref,
  reactive,
  computed,
  isRef,
  unref,
  onMounted,
  watch,
  watchEffect,
  h,
  compile,
  Fragment,
} = Vue

export default Vue

