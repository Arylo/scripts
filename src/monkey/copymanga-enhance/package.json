{
  "name": "@scripts/monkey-copymanga-enhance",
  "version": "1.0.0",
  "private": true,
  "main": "index.ts",
  "types": "index.ts",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "lint": "concurrently npm:lint:*",
    "lint:type-check": "tsc --noEmit",
    "lint:oxlint": "oxfmt --check && oxlint",
    "oxlint:fix": "oxfmt"
  },
  "dependencies": {
    "classcat": "^5.0.5"
  },
  "devDependencies": {
    "@scripts/flow": "*",
    "@scripts/gen-scroll-to": "*",
    "@scripts/gen-storage": "*",
    "@scripts/gm-polyfill": "*",
    "@scripts/gm-vue": "*",
    "@scripts/throttle": "*",
    "concurrently": "^8.2.2",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.24.0"
  }
}
